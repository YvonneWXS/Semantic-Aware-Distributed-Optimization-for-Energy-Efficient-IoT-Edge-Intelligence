你好！你现在是专业的 边缘计算代码工程师。我给你以下文件，请严格按照要求修改为ALE 和 ROE算法，先列出计划，我审核之后再启动修改

请按照以下结构为我重新生成 ALE 和 ROE 的代码：

参考资料：

1. D:\3_document\4_research\semantic_2\Semantic-Aware-Distributed-Optimization-for-Energy-Efficient-IoT-Edge-Intelligence\Local_Random（现有项目文件夹）
2. D:\3_document\4_research\semantic_2\Semantic-Aware-Distributed-Optimization-for-Energy-Efficient-IoT-Edge-Intelligence\带宽分配 - 基于离散权重的动态归一化分配（Proposed）.md（DW-DNA 完整方案）
3. D:\3_document\4_research\semantic_2\Semantic-Aware-Distributed-Optimization-for-Energy-Efficient-IoT-Edge-Intelligence\参数列表.md（所有固定参数必须严格保持一致）
4. 对ALE和ROE的描述
   + All local execution(ALE) scheme: All UEs’ computing tasks are executed locally.
   + All random execution(ROE) scheme: The decision on whether to offload computation tasks to the MEC server is made randomly. If offloading is chosen, both the semantic extraction factor and the allocation of computational resources are also randomly determined. This scheme does not consider task delay tolerance constraints, which serves as a relaxed modeling of system constraints. As a result, its performance is often significantly degraded in practical scenarios characterized by limited resources or stringent latency requirements compared to the simulation results.
5. 关于能耗计算等，可以参考"D:\3_document\4_research\semantic_2\semantic_code\SA_GA"文件夹下的计算方法，此算法为包含语义的遗传算法

项目结构要求：
- `env.py` （需要进行修改）
- 新建 `ALE.py`
- 新建 `ROE.py`
- 新建`parameter.py` 
- 新建修改 `main.py` 作为统一入口

**ALE (All Local Execution) 定义**：

- 所有 UE 的 offload_decision 强制为 0，强制本地执行
- w_bw = 0，semantic_factor = 1.0，resource_allocation = 0

**ROE (All Random Execution) 定义**（严格按照论文原文 + 审稿人要求）：
- offload_decision 完全随机（50% 概率为 1）
- 如果 offload=1，则：
  - semantic_factor 在 [0.3, 1.0] 内随机
  - resource_allocation 在 [0.01, 1.0] 内随机
  - w_bw 在 {1, 2, 3, 4} 中随机（必须大于 0）

**关于 parameter.py**

包含以下参数列表：
data_size_kb = [250, 500, 750, 1000, 1250, 1500, 1750, 2000]
num_of_ues = [5, 10, 15, 20, 25, 30]
bandwidth_khz = [750, 1000, 1250, 1500, 1750, 2000, 2250, 2500, 2750, 3000]
mec_capacity = [10.0, 12.5, 15.0, 17.5, 20.0, 22.5, 25.0, 27.5, 30.0]



**完整实现 DW-DNA 动态带宽分配机制**

- 动作空间改为 4 维离散动作：offload_decision (0/1), semantic_factor (k=100 离散), resource_allocation (k=100 离散), w_bw ∈ {0,1,2,3,4}。
- offload=0 时 w_bw 自动设为 0。
- 在 envpy 中新增 `compute_bandwidth_allocation(self, w_bw_list)` 方法，实现三阶段逻辑（归一化 + 量子化 + 剩余带宽损耗）。
- 更新 step() 中的上行速率公式使用动态 B_i，所有传输时延、能耗计算全部使用动态 B_i。

**实现要求**：

- `ALE.py` 和 `ROE.py` 各自独立成文件，只依赖 `env.py` 和 `parameter.py`
- 每个文件实现自己的 `run()` 函数，遍历 `parameter.num_of_ues`
- 每组配置运行 `parameter.EXPERIMENT_PARAMS["num_runs"]` 次
- 结果保存路径：
  - ALE → `results/ALE_{num_ues}UEs/run_{run_id}/`
  - ROE → `results/ROE_{num_ues}UEs/run_{run_id}/`
- 每个 run 目录必须包含：
  - `train_log.csv`（列：episode, energy, violation_rate）
  - `config.txt`（打印本次实验所有参数）
- 最终在各自目录生成汇总 `summary.csv`

**main.py 修改要求**：
- 作为统一入口
- 支持命令行参数选择运行 ALE 或 ROE（例如 `python main.py --baseline ALE` 或 `--baseline ROE`）
- 可以同时运行 ALE 和 ROE（默认同时运行）
- 支持批量实验：遍历 data_size_kb、num_of_ues、bandwidth_khz、mec_capacity
- 对每个配置创建 ENV → reset() → 运行完整流程。
